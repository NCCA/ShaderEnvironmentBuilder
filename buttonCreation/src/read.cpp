#include <iostream>
#include <fstream>
#include <cstdlib>
#include <string>

/*void readFile()
{
  std::cout<<"Working"<<std::endl;
  std::string line;
  std::ifstream myfile ("example.txt");
  if(myfile.is_open())
  {
    while(std::getline(myfile, line))
    {
      std::cout<<
    }
  }
}
*/
